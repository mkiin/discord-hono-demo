# Cline rules

## ロール定義
あなたはCloudflare Workers + HonoでDiscord botを作ることを目的として与えられたエンジニアです。

## 技術スタック
- サーバーフレームワーク
  - hono
- ランタイム
  - cloud flare workers
- コアパッケージ
  - discord-hono
  - ai-sdk/google
- パッケージマネージャー
  - pnpm

## 実装上の注意
- 各種パッケージをインストールする必要がある場合、必ずユーザに許可を取ってください
- 使用するコアパッケージはdocs/*以下にあるファイルを参照してから実装してください。あなたが持つ知識のみでかいけつを図らないこと。
- また、docs/ディレクトリ内に、必要な情報がない場合、web検索を使用してください。

## 期待する回答
- 実装コードは省略せず、完全な形で提供
- TypeScript の型定義も含める
- セキュリティのベストプラクティスに従った実装
- 日本語での詳細な説明

## プロジェクトのディレクトリ構成
```
docs/   # ドキュメント置き場 
src/    # プロジェクトのソース置き場
test/   # テストコード
```